# goit-algo-hw-02

Домашнє завдання до теми **“Основні структури даних”**.

У цьому репозиторії реалізовано практичні завдання з використанням:

- **Queue** (черга) — імітація обробки заявок
- **deque** (двостороння черга) — перевірка паліндрома
- **stack** (стек) — перевірка симетричності дужок _(необов’язково)_

---

## 📌 Структура репозиторію

```text
goit-algo-hw-02/
├── task_1.py   # Імітація системи обробки заявок (Queue)
├── task_2.py   # Перевірка рядка на паліндром (deque)
├── task_3.py   # Перевірка симетричності дужок (stack, необов’язково)
└── README.md
```

---

## ✅ Завдання 1 — **Черга (Queue)**

Програма імітує роботу сервісного центру:

- **автоматично генерує** заявки
- **додає** їх до черги
- **послідовно обробляє** заявки з черги _(FIFO)_

Використано клас **`Queue`** з модуля **`queue`**.

### ▶️ Запуск

**Windows (рекомендовано):**

```bash
py .\task_1.py
```

**macOS / Linux / або якщо працює `python` у PATH:**

```bash
python task_1.py
```

### ⛔ Зупинка

**`Ctrl + C`** _(або Kill Terminal у VS Code)_

---

## ✅ Завдання 2 — **Паліндром (deque)**

Функція перевіряє, чи є рядок паліндромом:

- **ігнорує регістр**
- **ігнорує пробіли**
- працює для _парної_ та _непарної_ кількості символів

Використано **`deque`** з модуля **`collections`**.

### ▶️ Запуск

**Windows (рекомендовано):**

```bash
py .\task_2.py
```

**macOS / Linux / або якщо працює `python` у PATH:**

```bash
python task_2.py
```

---

## ✅ Завдання 3 — **Симетричність дужок (stack)** _(необов’язково)_

Програма перевіряє коректність розміщення дужок:

- круглих **`()`**
- квадратних **`[]`**
- фігурних **`{}`**

Використано **стек** для зберігання відкритих дужок.

### ▶️ Запуск

**Windows (рекомендовано):**

```bash
py .\task_3.py
```

**macOS / Linux / або якщо працює `python` у PATH:**

```bash
python task_3.py
```

---

## 🧰 Вимоги

- **Python 3.x**
- **VS Code** або будь-яке інше середовище розробки
- Термінал (PowerShell / Terminal)

---

## 📝 Примітка

На **Windows** інколи команда `python` може бути прив’язана до системного ярлика.
У такому випадку використовуйте **`py`** для запуску скриптів.
